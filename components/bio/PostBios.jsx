import { StyleSheet, Text, SafeAreaView } from 'react-native';

export default function PostBios() {
  return (
    <SafeAreaView style={styles.container}>
      <Text>POST SCREEN</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

